# get_column()

    Code
      df$get_column("foo")
    Condition
      Error in `df$get_column()`:
      ! Evaluation failed in `$get_column()`.
      Caused by error:
      ! Column(s) not found: "foo" not found

